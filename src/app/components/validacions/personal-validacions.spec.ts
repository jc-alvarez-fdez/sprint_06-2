import { PersonalValidacions } from './personal-validacions';

describe('PersonalValidacions', () => {
  it('should create an instance', () => {
    expect(new PersonalValidacions()).toBeTruthy();
  });
});
